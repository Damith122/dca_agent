# Keyless paper validation only; never deploy this branch as a live trader.
[build]
builder = "RAILPACK"

[deploy]
startCommand = "python paper_validation.py --minutes 60"
restartPolicyType = "NEVER"
